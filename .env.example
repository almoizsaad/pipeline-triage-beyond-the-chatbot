# The graded workflow (/triage) needs no API keys and no backend — it runs
# entirely on the static sample dataset in src/triage/deals.ts.
#
# The variables below were used by the out-of-scope legacy `src/agent/`
# runtime (see NOTES.md → "Out of scope") and are not needed to run or
# deploy this submission.
# VITE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# VITE_API_BASE_URL=https://api.moonshot.cn/v1
